{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Portal",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "buildFlags": "-gcflags=\"all=-N -l\"",
            "dlvFlags": ["--check-go-version=false"],
            "showLog": false,
            "trace": "log"
        }
    ]
}